// Tipos de dados primitivos
const nome = 'Francisco'; //string
const nome1 = "Francisco"; //string
const nome2 = `Luiz`; //string (usando tampletes string)
const num = 10; //number
const num2 = 10.52; //number
let nomeAluno; //unidefined = não aponta para local nenhum na memoria
const sobrenomeAluno = null; //Null -> não aponta para local nenhum na memoria
const aprovado = true; //Valor booelan  = verdadeiro ou falso (lógico)

// Para saber o tipo de uma variável usa-se typeof
//exemplo
console.log(typeof num); // será impresso um "number";
